<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bluewind.index.dao.IIndexMapper">
	
	<select id="queryAddress" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT * FROM eshop_address WHERE is_default=1 LIMIT 0,10 
	</select>
	
	<select id="queryAddressForPage" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT * FROM eshop_address WHERE is_default=1 
	</select>

</mapper>